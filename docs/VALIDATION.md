# Agent 验证记录

## RRF 证据融合验证

61 项离线测试通过。新增验证覆盖分数公式、跨列表重复命中、单列表去重、稳定顺序、字节预算、空输入和非法参数；工作流集成测试确认融合元数据被保存，并且带 RRF 排序分数的证据传入汇总模型。未据此宣称事实准确率提升。

## 2026-09-12 自动编排与管理验证

最终离线结果：指定 lightrag 环境下 56 项 unittest 通过。

- 新增工作流与管理测试：规划、子任务执行、汇总自评、补充检索、有限重试、显式兜底、取消、失败保护、导入先于替换、评估策略和用量累计。
- 真实复杂问题拆为三个子任务，各自检索后汇总，并获得模型自评通过。此次 39.1 秒，15 次 LLM 调用、3 次工具调用，LLM 34642 Tokens，嵌入 50582 Tokens。
- 真实单题评估完成：基础 Agent 与工作流均命中来源，答案及证据关键词覆盖均为 100%；LLM 分别为 7119 和 9356 Tokens。单题结果不能证明整体质量提升。
- 独立 `outputs/qa/management-*` 索引验证文档导入、替换、删除成功；未对用户书籍索引执行这些写入/删除测试。
- 临时 SQLite：outputs/qa/workflow.sqlite3。实时任务信息和评估结果通过网页与 HTTP 检查；三个页面脚本通过 Node 语法检查。

## 前端布局与 Word 导出增量验证

- 43 项测试通过，新增了 Word MIME 类型、ZIP/XML 内容、章节链接与书签匹配及非法格式参数的检查。
- 实际浏览器查看了工作台布局和 Word/TXT 下载入口；JavaScript 语法检查通过。
- 使用现有任务生成 DOCX 样例。调用技能渲染器失败，原因是本机不存在 LibreOffice soffice.exe；未完成 Word 逐页视觉验证，不宣称分页验证通过。

## 2026-09-12 交互任务增量验证

- 指定 lightrag 环境下 43 项 unittest 全部通过，包括任务持久化、取消、进度读取、仅重试未完成部分、新版本、导出、删除和中断恢复。
- 工作台 JavaScript 通过 Node 语法检查。
- 使用临时 8091 服务与独立 `outputs/qa/interactive_tasks.sqlite3`，在浏览器中创建并运行“焦虑症有哪些主要症状？”资料整理任务。真实模型返回 completed，保存 4 条执行事件。
- 浏览器刷新恢复了任务与正文，页面排版已查看；HTTP 导出 TXT 与已保存正文一致。
- 此次真实模型验证为单项功能联调，不代表医学准确性评估。旧版检索测试记录见下文。

验证日期：2026-09-11。环境：`D:\Anaconda3\envs\lightrag\python.exe`。

## 离线验证

`python -m unittest discover -s tests -p "test_*.py"`：40 项通过。

覆盖工具参数校验、原文相邻块、图谱来源关联、单次与多次检索、重复调用抑制、工具/决策预算、虚构证据 ID 拒绝、模型/工具失败、取消及超时、上下文长度限制、多次搜索共享证据预算、API 模式切换、会话隔离、重启恢复、中断任务状态、级联删除和错误内容不污染历史。

额外验证了 LightRAG 队列为每个请求传播独立 ContextVar，以及调用者取消时底层协程结束、工作队列仍可处理后续调用。

Python 源文件编译语法检查和浏览器 JavaScript 的 `node --check` 均通过。Ruff 下载长时间未完成，已停止安装，本次未运行 Ruff；它列在 `requirements-dev.txt` 中供后续开发使用。

## 实际索引与网页

- `python -m rag_app.check` 成功加载现有索引并验证页面、健康检查和接口文档。
- 使用真实索引验证了原文相邻块读取、图谱查询及图谱来源文本关联，没有调用远程模型。
- 在临时本机端口 8091 上通过浏览器完成真实 Agent 问答，刷新后成功恢复对话，再问“我刚才的问题是什么”准确返回前一条问题。
- 临时测试会话使用 `outputs/qa/sessions.sqlite3`，未使用正式会话数据库。

## 真实模型对比样例

执行命令：

```powershell
python -m rag_app.evaluate --limit 1 --strategies "agent,hybrid+rerank" --cache-policy cold
```

问题：精神分裂症的病程标准是什么？

| 指标 | Agent | 普通 hybrid + rerank |
|---|---:|---:|
| 执行成功 | 是 | 是 |
| 答案关键词覆盖 | 100% | 100% |
| 原文证据关键词覆盖 | 100% | 100% |
| 预期来源命中 | 是 | 是 |
| 耗时 | 9.16 秒 | 8.28 秒 |
| LLM 调用次数（含关键词提取） | 4 | 2 |
| LLM 总 token | 7151 | 7437 |
| Embedding 调用次数 | 3 | 3 |
| Embedding token | 13203 | 12417 |
| Agent 工具调用次数 | 1 | 不适用 |

报告：[JSON](../outputs/evaluation/book_rag_eval_20260911_230155.json)、[CSV](../outputs/evaluation/book_rag_eval_20260911_230155.csv)。
这是单题联调，不能据此声称 Agent 整体效果更好或成本更低，也不是临床正确性评估。多轮检索和异常分支另由离线测试验证。

早期 `225656` 报告在联调发现并修复字符分词预算和队列用量上下文问题之前生成，不应作为最终对比结果。
